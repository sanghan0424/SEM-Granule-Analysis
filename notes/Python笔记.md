# Python for循环笔记（SEM颗粒分析场景）
## 核心用法
1. 遍历列表：for 元素 in 列表 → 遍历颗粒粒径列表
2. 遍历range()：for i in range(5) → 生成序号/批量处理
3. 遍历元组：for 元素 in 元组 → 固定业务阈值（如(0.5,5)）

## 颗粒分析常用示例
- 统计有效颗粒数：遍历粒径列表，计数符合条件的元素
- 批量处理SEM图像：遍历图像文件名，批量重命名/移动
